diff --git a/apps/server/src/coordinator/actionsExecution.ts b/apps/server/src/coordinator/actionsExecution.ts
index 023e8a6..21697b1 100644
--- a/apps/server/src/coordinator/actionsExecution.ts
+++ b/apps/server/src/coordinator/actionsExecution.ts
@@ -3,6 +3,7 @@
  * Phase 4 coordinator. See the design and blast-radius notes below the imports.
  */
 import { timingSafeEqual } from "node:crypto";
+import { nonce } from "../ids.js";
 import { NORNS_WORKFLOW_VERSION } from "../integrations/actionsWorkflowTemplate.js";
 import {
   type ActionsRepositoryRef,
@@ -11,7 +12,6 @@ import {
   enrollmentTokenHash,
   generateEnrollmentToken,
 } from "../integrations/githubActions.js";
-import { nonce } from "../ids.js";
 import type { V2SqlExecutor, V2TransactionRunner } from "../persistence/v2/database.js";
 import {
   type Phase4Coordinator,
diff --git a/apps/server/test/actionsDispatchConcurrency.test.ts b/apps/server/test/actionsDispatchConcurrency.test.ts
index 55a509f..2855acd 100644
--- a/apps/server/test/actionsDispatchConcurrency.test.ts
+++ b/apps/server/test/actionsDispatchConcurrency.test.ts
@@ -10,8 +10,8 @@ import { generateKeyPairSync } from "node:crypto";
 import { mkdtempSync } from "node:fs";
 import { tmpdir } from "node:os";
 import { join } from "node:path";
-import type { EventPayloadT, V2DispatchCommandT } from "@norns/contracts";
 import { PGlite } from "@electric-sql/pglite";
+import type { EventPayloadT, V2DispatchCommandT } from "@norns/contracts";
 import { RunnerDaemon } from "@norns/runner";
 import sodium from "libsodium-wrappers";
 import { afterEach, beforeEach, describe, expect, it } from "vitest";
@@ -38,7 +38,7 @@ import { GitHubActionsService } from "../src/integrations/githubActions.js";
 import { Phase7OperationsService } from "../src/operations/phase7Operations.js";
 import { PGliteTransactionRunner } from "../src/persistence/v2/database.js";
 import { type V2MigrationDatabase, runCurrentV2Migrations } from "../src/persistence/v2/migrate.js";
-import { authorizeProxiedRunAccess, SqlProxiedRunLookup } from "../src/runners/inferenceProxy.js";
+import { SqlProxiedRunLookup, authorizeProxiedRunAccess } from "../src/runners/inferenceProxy.js";
 import { type NornsServer, buildServer } from "../src/server.js";
 import { RelayStores } from "../src/stores.js";
 import { UserStore } from "../src/users/store.js";
@@ -219,7 +219,8 @@ async function buildStack(taskCount: number, cap: number): Promise<Stack> {
   const http = (async (input: string | URL | Request, init?: RequestInit) => {
     const url = String(input);
     const method = (init?.method ?? "GET").toUpperCase();
-    const body = typeof init?.body === "string" ? (JSON.parse(init.body) as Record<string, unknown>) : null;
+    const body =
+      typeof init?.body === "string" ? (JSON.parse(init.body) as Record<string, unknown>) : null;
     if (url.endsWith("/access_tokens")) {
       return json({
         token: `installation-token-${Math.random()}`,
@@ -277,7 +278,8 @@ async function buildStack(taskCount: number, cap: number): Promise<Stack> {
     actionsService,
     {
       serverOrigin: "https://norns.example",
-      runnerPackage: "0.1.0@sha256:aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa",
+      runnerPackage:
+        "0.1.0@sha256:aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa",
       reserveGeneration: (runnerId: string) => stores.reserveRunnerGeneration(runnerId),
     },
   );
@@ -291,7 +293,11 @@ async function buildStack(taskCount: number, cap: number): Promise<Stack> {
     stores,
     users,
     phase4,
-    actionsExecution: { coordinator: actionsCoordinator, enrollment, repository: actionsRepository },
+    actionsExecution: {
+      coordinator: actionsCoordinator,
+      enrollment,
+      repository: actionsRepository,
+    },
     phase7: { operations: new Phase7OperationsService(transactions) },
   });
   const url = await listen(server);
@@ -351,15 +357,16 @@ async function enrollAndConnect(
 }
 
 async function runState(stack: Stack, runId: string): Promise<string> {
-  const result = await stack.pg.query<{ state: string }>("SELECT state FROM agent_runs WHERE id = $1", [
-    runId,
-  ]);
+  const result = await stack.pg.query<{ state: string }>(
+    "SELECT state FROM agent_runs WHERE id = $1",
+    [runId],
+  );
   return result.rows[0]?.state ?? "(missing)";
 }
 
 describe("EXECUTION E5 — per-dispatch runner identity, end to end", () => {
   let stack: Stack | null = null;
-  let daemons: RunnerDaemon[] = [];
+  const daemons: RunnerDaemon[] = [];
 
   afterEach(async () => {
     for (const daemon of daemons.splice(0)) daemon.stop();
@@ -393,8 +400,14 @@ describe("EXECUTION E5 — per-dispatch runner identity, end to end", () => {
 
     // Both commands are actually delivered and both runs actually finish —
     // "run to completion", not merely "stay connected".
-    await waitFor(async () => (await runState(stack as Stack, jobA.run_id)) === "succeeded", "run A succeeded");
-    await waitFor(async () => (await runState(stack as Stack, jobB.run_id)) === "succeeded", "run B succeeded");
+    await waitFor(
+      async () => (await runState(stack as Stack, jobA.run_id)) === "succeeded",
+      "run A succeeded",
+    );
+    await waitFor(
+      async () => (await runState(stack as Stack, jobB.run_id)) === "succeeded",
+      "run B succeeded",
+    );
     expect(daemonA.isFenced).toBe(false);
     expect(daemonB.isFenced).toBe(false);
   }, 20_000);
@@ -478,7 +491,10 @@ describe("EXECUTION E5 — per-dispatch runner identity, end to end", () => {
     );
     expect(revoke.status).toBe(200);
 
-    await waitFor(() => !stack?.server.connectedRunners().includes(jobA.actions.runner_id), "runner A disconnected");
+    await waitFor(
+      () => !stack?.server.connectedRunners().includes(jobA.actions.runner_id),
+      "runner A disconnected",
+    );
     // The concurrent dispatch is untouched.
     expect(daemonB.isFenced).toBe(false);
     expect(stack.server.connectedRunners()).toContain(jobB.actions.runner_id);
@@ -497,15 +513,30 @@ describe("EXECUTION E5 — per-dispatch runner identity, end to end", () => {
 
     // Runner B's identity presenting for run A: refused.
     expect(
-      authorizeProxiedRunAccess(factsA, jobA.run_id, jobB.actions.runner_id, jobB.actions.runner_generation),
+      authorizeProxiedRunAccess(
+        factsA,
+        jobA.run_id,
+        jobB.actions.runner_id,
+        jobB.actions.runner_generation,
+      ),
     ).toBe("unauthorized");
     // Runner A's identity, but claiming run B: refused.
     expect(
-      authorizeProxiedRunAccess(factsB, jobA.run_id, jobA.actions.runner_id, jobA.actions.runner_generation),
+      authorizeProxiedRunAccess(
+        factsB,
+        jobA.run_id,
+        jobA.actions.runner_id,
+        jobA.actions.runner_generation,
+      ),
     ).toBe("unauthorized");
     // Runner A's own identity for its own run, at its own generation: allowed.
     expect(
-      authorizeProxiedRunAccess(factsA, jobA.run_id, jobA.actions.runner_id, jobA.actions.runner_generation),
+      authorizeProxiedRunAccess(
+        factsA,
+        jobA.run_id,
+        jobA.actions.runner_id,
+        jobA.actions.runner_generation,
+      ),
     ).toBe("ok");
   });
 
@@ -529,7 +560,10 @@ describe("EXECUTION E5 — per-dispatch runner identity, end to end", () => {
     await laptop.pair(pairing.code);
     laptop.connect();
     daemons.push(laptop);
-    await waitFor(() => stack?.server.connectedRunners().includes("laptop-1") ?? false, "laptop paired");
+    await waitFor(
+      () => stack?.server.connectedRunners().includes("laptop-1") ?? false,
+      "laptop paired",
+    );
 
     // Schedule and connect an Actions-hosted dispatch in the SAME server
     // instance while the laptop stays connected.
