{
  "name": "@norns/runner",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "TheNorns Local Runner: outbound relay client, buffered replay, command dedup, fixture executor",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "norns-runner": "dist/cli.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "echo '@norns/runner: covered by @norns/server integration tests'"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.3.207",
    "@norns/contracts": "workspace:*",
    "@openai/codex-sdk": "^0.144.3",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/ws": "^8.5.0",
    "typescript": "^5.5.0"
  }
}
