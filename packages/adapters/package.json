{
  "name": "@norns/adapters",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "TheNorns LLM adapters: Anthropic + OpenAI behind one interface, usage normalization, failure taxonomy",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "latest",
    "@norns/contracts": "workspace:*",
    "openai": "latest",
    "zod": "^3.23.8",
    "zod-to-json-schema": "^3.25.2"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vitest": "^3.0.0"
  }
}
