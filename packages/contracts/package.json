{
  "name": "@norns/contracts",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "TheNorns shared contracts: Plan Contract, node lifecycle, runner protocol, usage, approvals (single source of truth)",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vitest": "^3.0.0"
  }
}
